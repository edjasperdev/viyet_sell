%div.row
	%br
	%div.col-md-2.col-sm-3.text-center
		%img{:src =>"#{@product.image_url}", :style => "width:150px;height:150px", :class => "img-rounded"}
	%div.col-md-10.col-sm-9
		%h3 
			#{@product.name} 
			%br Now: $#{@product.price}
		%div.row
			%div.col-xs-9
				%h4
					%span.label.label-default Was: $#{@product.original_price}
				#{@product.furniture_type}

=link_to 'View All Products on Sale', products_path, :class => "label label-default"
=link_to 'List New Product', root_path, :class => "label label-default"
